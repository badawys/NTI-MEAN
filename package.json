{
  "name": "codes-training-center",
  "version": "1.0.0",
  "private": true,
  "description": "A teaching-focused MEAN stack MVP for Codes Training Center.",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev --workspace server\" \"npm start --workspace client\"",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "seed": "npm run seed --workspace server",
    "start:api": "npm run dev --workspace server",
    "start:web": "npm start --workspace client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=22.22.3"
  }
}
